# ===================================
#  Form Field Definitions
# ===================================

fields:
    ip_address:
        label: IP address
        span: left
        comment: Specify an IP address to check.
    end_ip_address:
        label: End IP address
        span: right
        comment: Specify an IP address for the end of the internal IP network range.
