# ===================================
#  Form Field Definitions
# ===================================

fields:
    start_ip_address:
        label: Start IP address
        span: left
        comment: Specify an IP address for the start of the internal IP network range.
    end_ip_address:
        label: End IP address
        span: right
        comment: Specify an IP address for the end of the internal IP network range.
